[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --ignore-scripts"]

[phases.build]
cmds = ["npm run build --workspace=apps/api"]

[start]
cmd = "node apps/api/dist/main"

[options]
no-cache = true
